# vim:set ft=conf

# Use screen-style binding so we can nest a session from OS X
# Uncomment / move into ~/.tmux.local to enable
# unbind C-b
# unbind-key b

# set -g prefix C-a

# # Prefix twice to send prefix
# bind-key a send-prefix
