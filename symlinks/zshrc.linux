# TODO: Check which of these are needed

# Default terminal color depth is pathetic
if [ "$TERM" = "xterm" ]; then
  export TERM=xterm-256color
fi

# if [ -z $SSH_AGENT_PID ]; then
#   eval `ssh-agent -s` > /dev/null
# fi

