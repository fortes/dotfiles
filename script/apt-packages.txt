# Debian/apt packages required by setup_linux and the Docker image
# Lines starting with # are ignored

# For yazi
7zip
apt-file
apt-transport-https
apt-utils
aptitude
asciinema
bash-completion
bat
# Codex sandbox requirement
bubblewrap
build-essential
ca-certificates
cargo
chafa
curl
dnsutils
duf
eza
fd-find
ffmpeg
# Required for `yazi`
file
# Debian version ancient, get from `install_github_packages`
# fzf
gh
git
git-crypt
git-extras
git-lfs
git-delta
gnupg
gocryptfs/trixie-backports
gron
htop
# Debian version ancient, get from `install_github_packages`
# hugo
iftop
imagemagick
info
jq
keychain
less
# marksman is a .NET binary and aborts at startup without ICU. Nothing else
# here pulls it in. The version number tracks the Debian release, and every
# package in this file goes through one `apt-get install`, so on the next
# release this name won't resolve and the whole install will abort with
# `E: Unable to locate package` — bump it then. (`libicu-dev` would be a stable
# name, but drags in 51MB of headers and static libs for a runtime-only need.)
libicu76
locales
lsb-release
lsof
man-db
moreutils
ncal
ncdu
ncurses-term
# Installed via `install_github_packages`
# neovim
pipx
python3
python3-dev
python3-pip
python3-proselint
python3-pygments
python3-pylast
python3-venv
python3-virtualenv
ripgrep
rsync
shellcheck/trixie-backports
shfmt
ssh
ssh-import-id
sshfs
stow
sudo
tmux/trixie-backports
trash-cli
unzip
wamerican
wget
yank
yq/trixie-backports
yt-dlp/trixie-backports
zoxide
