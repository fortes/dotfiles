# Debian/apt packages required by setup_linux and the Docker image
# Lines starting with # are ignored

# For yazi
7zip
apt-file
apt-transport-https
apt-utils
aptitude
bash-completion
bat
# Codex sandbox requirement
bubblewrap
build-essential
ca-certificates
cargo
chafa
curl
dnsutils
duf
eza
fd-find
ffmpeg
# Required for `yazi`
file
# Debian version ancient, get from `install_github_packages`
# fzf
# Debian version ancient, get from `install_github_packages`
# gh
git
git-crypt
git-extras
git-lfs
git-delta
gnupg
gocryptfs/trixie-backports
gron
htop
# Debian version ancient, get from `install_github_packages`
# hugo
iftop
imagemagick
info
jq
keychain
less
# marksman is a .NET binary and aborts at startup without ICU; nothing else
# here pulls it in. The version is part of the package name and tracks the
# Debian release, so bump it on upgrade — everything in this file goes through
# a single `apt-get install`, so a name that no longer resolves aborts the lot.
libicu76
locales
lsb-release
lsof
man-db
moreutils
ncal
ncdu
ncurses-term
# Installed via `install_github_packages`
# neovim
pipx
python3
python3-dev
python3-pip
python3-proselint
python3-pygments
python3-pylast
python3-venv
python3-virtualenv
ripgrep
rsync
shellcheck/trixie-backports
shfmt
ssh
ssh-import-id
sshfs
stow
sudo
tmux/trixie-backports
trash-cli
unzip
wamerican
wget
yank
# Debian ships a different `yq` (a python jq wrapper with an incompatible
# CLI), get mikefarah's from `install_github_packages`
# yq
yt-dlp/trixie-backports
zoxide
