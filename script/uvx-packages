# vim:ft=sh
#
# Must force python version due to dependency conflict
aider-chat --python python3.12
argcomplete
beautysh
# Requires additional dependencies due to plugins
beets --with requests --with pylast
epy-reader
llm
pyright
ruff
vim-vint
